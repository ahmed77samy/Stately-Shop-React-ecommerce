/**
 * run the route service with path and component
 * @param {Function} route 
 */
export default function shopRoutes (route) {
    // route("/collections-grid" , Collections);
    // route("/products" , Products);
}